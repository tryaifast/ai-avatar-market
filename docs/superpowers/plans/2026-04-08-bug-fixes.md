# Bug修复实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development

**Goal:** 修复AI Avatar Market项目的关键Bug，使核心功能正常工作

**Architecture:** 最小化修改，只修复问题不重构架构，保持现有代码结构

**Tech Stack:** Next.js 14, TypeScript, React, Tailwind CSS

---

## 任务清单

### Task 1: 修复API认证错误处理
**文件:** `app/api/auth/route.ts`  
**时间:** 5分钟  
**内容:**
- 添加try-catch包裹所有API逻辑
- 确保所有错误返回JSON格式
- 验证Content-Type header

```typescript
// 完整代码
import { NextRequest, NextResponse } from 'next/server';
import bcrypt from 'bcryptjs';
import { UserDB } from '@/lib/db';
import { signToken } from '@/lib/jwt';

export async function POST(request: NextRequest) {
  try {
    const { action, ...data } = await request.json();
    
    if (action === 'register') {
      const { email, password, name, role } = data;
      
      if (!email || !password || !name) {
        return NextResponse.json(
          { error: 'Missing required fields' },
          { status: 400 }
        );
      }
      
      const existingUser = UserDB.findByEmail(email);
      if (existingUser) {
        return NextResponse.json(
          { error: 'Email already registered' },
          { status: 409 }
        );
      }
      
      const hashedPassword = await bcrypt.hash(password, 10);
      const user = UserDB.create({
        email,
        password: hashedPassword,
        name,
        role: role || 'client',
      });
      
      const token = signToken({ userId: user.id, email: user.email, role: user.role });
      
      return NextResponse.json({
        token,
        user: { id: user.id, email: user.email, name: user.name, role: user.role },
      });
    }
    
    if (action === 'login') {
      const { email, password } = data;
      
      if (!email || !password) {
        return NextResponse.json(
          { error: 'Email and password required' },
          { status: 400 }
        );
      }
      
      const user = UserDB.findByEmail(email);
      if (!user || !user.password) {
        return NextResponse.json(
          { error: 'Invalid credentials' },
          { status: 401 }
        );
      }
      
      const isValid = await bcrypt.compare(password, user.password || '');
      if (!isValid) {
        return NextResponse.json(
          { error: 'Invalid credentials' },
          { status: 401 }
        );
      }
      
      const token = signToken({ userId: user.id, email: user.email, role: user.role });
      
      return NextResponse.json({
        token,
        user: { id: user.id, email: user.email, name: user.name, role: user.role },
      });
    }
    
    return NextResponse.json(
      { error: 'Invalid action' },
      { status: 400 }
    );
  } catch (error) {
    console.error('Auth API error:', error);
    return NextResponse.json(
      { error: error instanceof Error ? error.message : 'Internal server error' },
      { status: 500 }
    );
  }
}
```

**测试命令:** `npm run build`  
**验证步骤:** 检查无TypeScript错误

---

### Task 2: 修复useAuth Hook错误处理
**文件:** `lib/hooks/useAuth.tsx`  
**时间:** 5分钟  
**内容:**
- 修复login/register的错误解析
- 添加响应状态检查
- 确保错误消息正确显示

```typescript
// 修复后的login函数
const login = async (email: string, password: string) => {
  try {
    setLoading(true);
    setError(null);
    
    const response = await fetch('/api/auth', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ action: 'login', email, password }),
    });
    
    // 检查Content-Type
    const contentType = response.headers.get('content-type');
    if (!contentType || !contentType.includes('application/json')) {
      throw new Error('Server returned non-JSON response. Please try again later.');
    }
    
    const data = await response.json();
    
    if (!response.ok) {
      throw new Error(data.error || `HTTP error! status: ${response.status}`);
    }
    
    localStorage.setItem('token', data.token);
    localStorage.setItem('user', JSON.stringify(data.user));
    setUser(data.user);
    router.push(data.user.role === 'creator' ? '/creator/dashboard' : '/client/market');
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Login failed';
    setError(message);
    throw err;
  } finally {
    setLoading(false);
  }
};

// 修复后的register函数
const register = async (userData: { email: string; password: string; name: string; role: string }) => {
  try {
    setLoading(true);
    setError(null);
    
    const response = await fetch('/api/auth', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ action: 'register', ...userData }),
    });
    
    const contentType = response.headers.get('content-type');
    if (!contentType || !contentType.includes('application/json')) {
      throw new Error('Server returned non-JSON response. Please try again later.');
    }
    
    const data = await response.json();
    
    if (!response.ok) {
      throw new Error(data.error || `HTTP error! status: ${response.status}`);
    }
    
    localStorage.setItem('token', data.token);
    localStorage.setItem('user', JSON.stringify(data.user));
    setUser(data.user);
    router.push(data.user.role === 'creator' ? '/creator/dashboard' : '/client/market');
  } catch (err) {
    const message = err instanceof Error ? err.message : 'Registration failed';
    setError(message);
    throw err;
  } finally {
    setLoading(false);
  }
};
```

**测试命令:** `npm run build`  
**验证步骤:** 检查无编译错误

---

### Task 3: 修复头像上传功能
**文件:** `app/creator/avatar/create/page.tsx`  
**时间:** 5分钟  
**内容:**
- 添加正确的文件input和onChange处理
- 实现文件上传逻辑
- 添加预览功能

```typescript
// 在表单组件中添加
const [avatarFile, setAvatarFile] = useState<File | null>(null);
const [avatarPreview, setAvatarPreview] = useState<string>('');

const handleAvatarChange = (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0];
  if (file) {
    setAvatarFile(file);
    const reader = new FileReader();
    reader.onloadend = () => {
      setAvatarPreview(reader.result as string);
    };
    reader.readAsDataURL(file);
  }
};

// 在JSX中添加input
<input
  type="file"
  accept="image/*"
  onChange={handleAvatarChange}
  className="hidden"
  id="avatar-upload"
/>
<label
  htmlFor="avatar-upload"
  className="cursor-pointer px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600"
>
  上传头像
</label>
{avatarPreview && (
  <img src={avatarPreview} alt="Preview" className="w-20 h-20 rounded-full object-cover mt-2" />
)}
```

**测试命令:** `npm run dev`  
**验证步骤:** 点击上传按钮弹出文件选择

---

### Task 4: 修复侧边栏导航
**文件:** `app/creator/layout.tsx`  
**时间:** 5分钟  
**内容:**
- 检查所有导航链接的href
- 确保路由配置正确
- 使用正确的Next.js Link组件

```typescript
// 确认侧边栏菜单项
const menuItems = [
  { href: '/creator/dashboard', icon: LayoutDashboard, label: '总览' },
  { href: '/creator/avatars', icon: Bot, label: '我的分身' },
  { href: '/creator/tasks', icon: ClipboardList, label: '任务管理' },
  { href: '/creator/earnings', icon: DollarSign, label: '收益' },
  { href: '/creator/messages', icon: MessageSquare, label: '消息' },
  { href: '/creator/settings', icon: Settings, label: '设置' },
];

// 确保使用Link组件
<Link href={item.href}>
  <div className={cn('flex items-center gap-3 px-4 py-3 rounded-lg', pathname === item.href && 'bg-blue-50 text-blue-600')}>
    <item.icon className="w-5 h-5" />
    <span>{item.label}</span>
  </div>
</Link>
```

**测试命令:** 点击每个菜单  
**验证步骤:** 所有链接能正确跳转

---

### Task 5: 创建缺失页面路由
**文件:** 多个  
**时间:** 10分钟  
**内容:**
创建缺失的页面文件：
1. `app/creator/avatars/page.tsx` - 我的分身
2. `app/creator/tasks/page.tsx` - 任务管理  
3. `app/creator/earnings/page.tsx` - 收益
4. `app/creator/messages/page.tsx` - 消息
5. `app/creator/settings/page.tsx` - 设置
6. `app/client/hire/[id]/page.tsx` - 雇佣页面

```typescript
// 模板代码（每个页面）
export default function Page() {
  return (
    <div className="p-6">
      <h1 className="text-2xl font-bold mb-4">页面标题</h1>
      <p>功能开发中...</p>
    </div>
  );
}
```

**测试命令:** `npm run build`  
**验证步骤:** 所有路由无404

---

### Task 6: 修复雇佣按钮路由
**文件:** `app/client/market/page.tsx`  
**时间:** 3分钟  
**内容:**
- 确保雇佣按钮链接到正确路由
- 添加 Link 包裹

```typescript
<Link href={`/client/hire/${avatar.id}`}>
  <Button>雇佣</Button>
</Link>
```

**测试命令:** 点击雇佣按钮  
**验证步骤:** 正确跳转到雇佣页面

---

### Task 7: 添加错误边界
**文件:** `components/ErrorBoundary.tsx` (新建)  
**时间:** 5分钟  
**内容:**
- 创建React错误边界组件
- 在layout中包裹子组件

```typescript
'use client';

import React from 'react';

export class ErrorBoundary extends React.Component<
  { children: React.ReactNode },
  { hasError: boolean; error?: Error }
> {
  constructor(props: { children: React.ReactNode }) {
    super(props);
    this.state = { hasError: false };
  }

  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: React.ErrorInfo) {
    console.error('Error caught by boundary:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="p-8 text-center">
          <h2 className="text-xl font-bold text-red-600 mb-2">出错了</h2>
          <p className="text-gray-600 mb-4">{this.state.error?.message}</p>
          <button
            onClick={() => this.setState({ hasError: false })}
            className="px-4 py-2 bg-blue-500 text-white rounded"
          >
            重试
          </button>
        </div>
      );
    }

    return this.props.children;
  }
}
```

**测试命令:** `npm run build`  
**验证步骤:** 编译成功

---

### Task 8: 更新layout使用错误边界
**文件:** `app/layout.tsx`  
**时间:** 3分钟  
**内容:**
- 导入ErrorBoundary
- 包裹主要内容

```typescript
import { ErrorBoundary } from '@/components/ErrorBoundary';

// 在body中
<ErrorBoundary>
  {children}
</ErrorBoundary>
```

**测试命令:** `npm run build`  
**验证步骤:** 编译成功

---

## 提交计划

1. `git add .` - 添加所有修改
2. `git commit -m "fix: 修复API错误处理和认证功能"` - Task 1-2
3. `git commit -m "fix: 修复头像上传和侧边栏导航"` - Task 3-4
4. `git commit -m "feat: 添加缺失页面和错误边界"` - Task 5-8
5. `git push origin master` - 推送到GitHub

## 验证清单

- [ ] 注册功能正常
- [ ] 登录功能正常
- [ ] 头像上传可用
- [ ] 所有导航链接有效
- [ ] 雇佣按钮跳转正确
- [ ] 页面不崩溃
- [ ] 无控制台错误
